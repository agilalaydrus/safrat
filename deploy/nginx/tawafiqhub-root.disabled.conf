# Intentionally empty.
#
# The VPS still has an enabled symlink named `tawafiqhub-root`. The canonical
# apex, www redirect, and legacy app redirect now live together in
# /etc/nginx/sites-available/tawafiqhub so their ownership and rollback are
# atomic. Keeping this tracked neutral file avoids deleting VPS-managed links
# while preventing the former root-to-app redirect from conflicting.
