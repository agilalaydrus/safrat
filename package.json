{
  "name": "hajj-umrah-operator-saas",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "buf:generate": "cd proto && buf generate",
    "buf:breaking": "cd proto && buf breaking --against '.git#branch=main'"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
